<div class="contents row">
  <% @infos.each do |info| %>
    <div class="content_post" style="background-image: url(<%= info.image %>);">
      <p><%= info.text %></p>
      <span class="name">
        <%= info.name %>
      </span>
    </div>
  <% end %>
</div>